<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en">
    <title>{{ config.title }}</title>
    <link href="{{ get_url(path="index.xml", trailing_slash=false) }}" rel="self" type="application/atom+xml"/>
    <link href="{{ config.base_url }}"/>
    <generator uri="https://www.getzola.org/">Zola</generator>
    <updated>{% if last_updated %}{{ last_updated | date(format="%+") }}{% else %}{{ now() | date(format="%+") }}{% endif %}</updated>
    <id>{{ config.base_url }}/</id>
    {% for page in pages %}
    <entry xml:lang="en">
        <title>{{ page.title }}</title>
        <published>{{ page.date | date(format="%+") }}</published>
        <updated>{% if page.updated %}{{ page.updated | date(format="%+") }}{% else %}{{ page.date | date(format="%+") }}{% endif %}</updated>
        <link rel="alternate" href="{{ page.permalink }}" type="text/html"/>
        <id>{{ page.permalink }}</id>
        <content type="html">{{ page.content | striptags }}</content>
    </entry>
    {% endfor %}
</feed>
